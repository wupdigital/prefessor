apply plugin: 'digital.wup.android-maven-publish'

publishing {
    publications {
        prefessor(MavenPublication) {

            if (components.findByName('android')) {
                from components.android
            } else if (components.findByName('java')) {
                from components.java
            }
        }
    }
}